<!DOCTYPE html>
<html lang="pt-br">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Painel Aluno</title>

  <!-- Estilos -->
  <link rel="stylesheet" href="/style.css" />
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet"
    integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC"
    crossorigin="anonymous" />
  <link rel="stylesheet"
    href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css" />

  <!-- Script -->
  <script src="/index.js" defer></script>

</head>
<body class="bg-light">

  <%- include('partials/navbar') %>

  <main class="container py-4">
    <div class="row">
      <div class="col-12">
        <h4 class="text-center mb-4">Avaliação</h4>
      </div>
    </div>

    <div>
      <h6><a href="/painel-aluno"><i class="bi bi-box-arrow-left" title="Voltar"></i></a> Aluno(a): <strong><%= usuario.nome %></strong></h6>
    </div>



      <!-- EXIBINDO MENSAGEM QUANDO NÃO TIVER NOTA REGISTRADA -->


      <% if(typeof avaliacoes === " " || avaliacoes === "undefinid" || avaliacoes === "NULL") { %>

          <h5>Nenhuma nota registrada no momento. </h5>


        <% } %>

     <% avaliacoes.forEach((aluno, index) => { %>
  <div class="accordion" style="padding: 10px;" id="accordionExample<%= index %>">
    <div class="accordion-item">
      <h2 class="accordion-header" id="heading<%= index %>">
        <button class="accordion-button <%= index !== 0 ? 'collapsed' : '' %>" type="button" data-bs-toggle="collapse"
          data-bs-target="#collapse<%= index %>" aria-expanded="<%= index === 0 %>" aria-controls="collapse<%= index %>">
          <%= aluno.periodo %>
        </button>
      </h2>
      <div id="collapse<%= index %>" class="accordion-collapse collapse <%= index === 0 ? 'show' : '' %>" aria-labelledby="heading<%= index %>">
        <div class="accordion-body">
          
          <form>
       
            <div class="row g-3 mb-4">
              <div class="col-12 col-md-4">
                <label class="form-label">Nome do Aluno</label>
                <input type="text" class="form-control" value="<%= aluno.aluno %>" readonly />
              </div>
              <div class="col-12 col-md-4">
                <label class="form-label">Período</label>
                <input type="text" class="form-control" value="<%= aluno.periodo %>" readonly />
              </div>
              <div class="col-12 col-md-4">
                <label class="form-label">Professor Avaliador</label>
                <input type="text" class="form-control" value="<%= aluno.professor %>" readonly />
              </div>
            </div>

         
            <div class="table-responsive mb-4">
              <table class="table table-bordered text-center align-middle">
                <thead class="table-secondary">
                  <tr>
                    <th>Soft Skill</th>
                    <th>Descrição</th>
                    <th style="width: 90px;">Nota</th>
                  </tr>
                </thead>
                <tbody>
                  <tr>
                    <td>Comunicação</td>
                    <td>Clareza ao se expressar, ouvir com atenção, participação em discussões.</td>
                    <td><input type="number" min="0" max="5" class="form-control" value="<%= aluno.comunicacao %>" readonly /></td>
                  </tr>
                  <tr>
                    <td>Trabalho em Equipe</td>
                    <td>Colaboração, respeito e contribuição em grupo.</td>
                    <td><input type="number" min="0" max="5" class="form-control" value="<%= aluno.trabalho_equipe %>" readonly /></td>
                  </tr>
                  <tr>
                    <td>Responsabilidade</td>
                    <td>Comprometimento, pontualidade, ética.</td>
                    <td><input type="number" min="0" max="5" class="form-control" value="<%= aluno.responsabilidade %>" readonly /></td>
                  </tr>
                  <tr>
                    <td>Pensamento Crítico</td>
                    <td>Argumentação, questionamento, resolução de problemas.</td>
                    <td><input type="number" min="0" max="5" class="form-control" value="<%= aluno.pensamento_critico %>" readonly /></td>
                  </tr>
                  <tr>
                    <td>Proatividade</td>
                    <td>Iniciativa e autonomia para resolver tarefas.</td>
                    <td><input type="number" min="0" max="5" class="form-control" value="<%= aluno.proatividade %>" readonly /></td>
                  </tr>
                  <tr>
                    <td>Liderança</td>
                    <td>Influência positiva, tomada de decisões.</td>
                    <td><input type="number" min="0" max="5" class="form-control" value="<%= aluno.lideranca %>" readonly /></td>
                  </tr>
                  <tr>
                    <td>Adaptabilidade</td>
                    <td>Flexibilidade e aceitação de mudanças.</td>
                    <td><input type="number" min="0" max="5" class="form-control" value="<%= aluno.adaptabilidade %>" readonly /></td>
                  </tr>
                  <tr>
                    <td>Empatia</td>
                    <td>Respeito e compreensão dos sentimentos dos outros.</td>
                    <td><input type="number" min="0" max="5" class="form-control" value="<%= aluno.empatia %>" readonly /></td>
                  </tr>
                </tbody>
              </table>
            </div>

            <div class="mb-4" style="width: 200px;">
              <label class="form-label">Média:</label>
              <input type="text" class="form-control" value="<%= aluno.media %>" readonly />
            </div>

            <div class="mb-4">
              <label class="form-label">Observações Gerais</label>
              <textarea class="form-control" rows="4" readonly><%= aluno.observacao %></textarea>
            </div>
          </form>
        </div>
      </div>
    </div>
  </div>
<% }) %>

  </main>

  <%- include('partials/footer') %>

  <!-- Scripts do Bootstrap -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/js/bootstrap.bundle.min.js"
    integrity="sha384-MrcW6ZMFYlzcLA8Nl+NtUVF0sA7MsXsP1UyJoMp4YLEuNSfAP+JcXn/tWtIaxVXM"
    crossorigin="anonymous"></script>
</body>
</html>
